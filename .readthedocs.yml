version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - requirements: requirements.txt

mkdocs:
  configuration: mkdocs.yml

# Custom pre-build step to clone folders into docs/
build.commands:
  - python clone_directories_to_docs.py
